name : my-first-app

on: push

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: checkout repo
              uses: actions/checkout@v4
              
            - name: List and Read
              run: |
                echo "Hi there , how are you"  
                ls -ltra
                cat README.md

                  

